# Environment configuration for Docker deployment

# Frontend API URL (change this for production)
# Local Docker: http://backend:5000 (Docker internal networking)
REACT_APP_API_URL=http://backend:5000

# Flask configuration
FLASK_ENV=production
FLASK_APP=app.py

