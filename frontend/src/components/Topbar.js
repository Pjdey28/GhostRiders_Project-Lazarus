import { Box, Typography } from "@mui/material";

export default function Topbar() {
  return (
    <Box mb={2}>
      <Typography variant="h4">Hospital Intelligence System</Typography>
    </Box>
  );
}